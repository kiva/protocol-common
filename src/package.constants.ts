// The reason this is separate from constants is its not really meant to be exported, only available
// to other types in this package.
export class PackageConstants {
    public static readonly ENV_SOURCE: string = 'ENV_SOURCE'
}
